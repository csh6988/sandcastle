---
"@chenshaohui6988/sandcastle": minor
---

Add immutable versioned Test Case revisions and Test Runs that consume exact PASS Integration Generation authority, freeze deterministic Test-scope risk inputs, correlate public UI and authoritative Runtime assertions with verified Artifact evidence, reconcile accepted external test effects safely, route evidence-backed defects into fresh rework runs, and expose PASS Test authority through Desktop Runtime commands, queries, events, and reloadable views. The scoped Electron fixture now proves the formal Runtime/Artifact flow and cleanup boundary without direct driver database writes.
