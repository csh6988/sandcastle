---
"@chenshaohui6988/sandcastle": minor
---

Add immutable versioned Test Case revisions and Test Runs that consume exact PASS Integration Generation and Build Artifact lineage, apply the complete Runtime-owned risk rubric, persist and compare exact UI/Runtime observations, close over failed/unknown/missing assertions, bind evidence to the exact acknowledged Query chain, reconcile external effects atomically, and resolve the complete frozen rework scope through explicit successor mappings. Expose PASS Test authority through Desktop Runtime commands, queries, events, and reloadable views. The scoped Electron fixture proves the formal Runtime/Artifact flow, race-safe evidence identity checks, quarantine-based cleanup, real preload-mediated interaction, reload recovery, and the production fixture boundary without direct driver database writes.
