---
"@chenshaohui6988/sandcastle": patch
---

Fix workspace `no-sandbox` session capture so host-written Claude session files are copied through the wrapper instead of leaving capture temp files missing.
