---
"@chenshaohui6988/sandcastle": minor
---

Bump the Codex default model from `gpt-5.4` to `gpt-5.5` in `sandcastle init` scaffolding, the interactive agent picker, and the `workspace plan/run/execute` default (all sourced from the agent registry). Pass `--model` to override.
